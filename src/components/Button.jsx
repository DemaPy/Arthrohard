









const Button = ({ bgColor }) => {
  return (
    <button className={`${bgColor} text-white md:px-10 px-8 md:py-3 py-2 futura-hero-button xl:w-fit w-full`}>Kup teraz</button>
  )
}

export default Button